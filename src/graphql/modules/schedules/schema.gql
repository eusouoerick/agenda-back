type Schedules {
  _id: ID!
  createdBy: User!
  service: Service!
  month: Int!
  day: Int!
  hour: Float!
}

input SchedulesInput {
  id: ID
  createdBy: ID
  service: ID
  month: Int
  day: Int
  hour: Float
}

type Query {
  schedules: [Schedules!]
  getSchedule(id: ID!): Schedules
  schedulesByService(id: ID): [Schedules]
  schedulesByUser(id: ID): [Schedules]
}

type Mutation {
  createSchedule(data: SchedulesInput): Schedules
  updateSchedule(data: SchedulesInput): Schedules
  deleteSchedule(id: ID): ID
}